# SPDX-FileCopyrightText: 2026 Mattia Egloff <mattia.egloff@pm.me>
#
# SPDX-License-Identifier: GPL-3.0-or-later

# CI local testing
.gitlab-ci-local/

# Build outputs
.build/

# Local xcframework builds (produced by prepare-device-test), which
# prepare-device-test symlinks rather than copies — a trailing slash
# here would only match a real directory and miss the symlink.
VauchiPlatformFFI.xcframework
